<resources>

    <!-- Base application theme. -->
    <style name="MyTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="MyThemeBottom" parent="MyTheme">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/cyan_dark</item>
        <item name="colorPrimaryDark">@color/cyan</item>
        <item name="colorAccent">@color/cyan_light</item>
    </style>

    <style name="MyTextHeaderListView">
        <item name="android:gravity">center|left</item>
        <item name="android:background">@color/cyan50</item>
        <item name="android:paddingLeft">16dp</item>
        <item name="android:paddingRight">16dp</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/grey_800</item>
    </style>

</resources>
